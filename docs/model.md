# O/R/T/C model

An `InvocationPlan` requires an explicit objective, identified resources, a tolerance that includes a risk level and loss boundary, and explicit constraints. Validation is deterministic and intentionally small.
